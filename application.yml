server:
  port: 8002
spring:
  application:
    name: service-provider
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mp?useSSL=false&serverTimezone=Asia/Shanghai
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: root
#使用consul注册中心
#  cloud:
#    consul:
#      host: localhost
#      port:  8500
#      discovery:
#        register: true
#        instance-id: ${spring.application.name}:${server.port}
#        prefer-ip-address: true
#        service-name: ${spring.application.name}
#        port: ${server.port}
#        ip-address: ${spring.cloud.client.ip-address}
#使用zookeeper注册中心
#  cloud:
#    zookeeper:
#      connect-string: 192.168.239.130:2181 #指定zookeeper的连接uri
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.angel.bean
  configuration:
    map-underscore-to-camel-case: true
#使用eureka注册中心
eureka:
  client:
    service-url:
      defaultZone: http://eureka.server7001:7001/eureka,http://eureka.server7002:7002/eureka
    fetch-registry: true #是否拉取服务
  instance:
    prefer-ip-address: true
management:
  endpoints:
    web:
      exposure:
        include: '*'